# エコーループ対策ロードマップ

## 背景

WebRTC 移行により割り込み（barge-in）が可能になったが、AI の音声がスピーカーからマイクに回り込み、
AI が自分の声に反応する「エコーループ」が発生する。
割り込みを維持しつつエコーだけを除去する対策が必要。

## 共通基盤

`_play_audio` でスピーカーに書き込むデータを **リングバッファ** にコピーし、参照信号として保持する。
Step 1・Step 2 のどちらでも必要。

## Step 1: 自己相関エコー検出 ← 現在ここ

再生済み音声とマイク入力の相互相関でエコーかどうかを判定し、エコーフレームのみ抑制する。

- 相関が高い → エコー → 抑制（減衰 or 無音化）
- 相関が低い → ユーザーの声 → そのまま通す
- **割り込み対応**: ユーザーの声は再生音と相関しないので通る
- **実装**: numpy のみ（correlate）
- **判断ポイント**: 実環境でエコーループが止まり割り込みも効くなら十分

## Step 2: NLMS 適応フィルタ（Step 1 で不十分な場合）

「部屋の伝達関数」をリアルタイムで学習し、エコー波形を予測して減算する。
Step 1 の「検出して抑制」ではなく「推定して除去」。

- 参照信号（リングバッファ）から適応フィルタでエコーを予測
- マイク入力からエコー予測を減算 → ユーザー声のみ残る
- 環境変化に自動追従
- ダブルトーク検出（ユーザー発話中はフィルタ更新を抑制）が必要
- **実装**: numpy のみ（NLMS アルゴリズム）
